# Pull Request–Driven CI Pipeline with Quality Gates

## Objective
To ensure only CI-validated code is merged into the main branch using GitHub Actions.

## Workflow
- Developer works on test branch
- Pull Request is created to main
- CI pipeline runs automatically
- Merge is blocked if CI fails

## Tools Used
Git, GitHub Actions, Linux, Shell Scripting
